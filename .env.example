# Copy to .env and fill in values. .env is gitignored — never commit it.
# Load before running Terraform: source .env

export TF_VAR_snowflake_private_key="$(cat keys/rsa_key.p8)"

# Uncomment if the key was generated with a passphrase:
# export TF_VAR_snowflake_private_key_passphrase="changeme"
